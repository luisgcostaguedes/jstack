// uso de qualquer 'aspas' é valido para a mesma coisa, agora quando usamos a `crase` podemos interpolar tudo no JS


const exemploDeLinha = 'uma linha apenas';

const linhas = `aqui podemos dar enter e vai ser respeitado o mesmo
segue o fioooo`;

console.log(exemploDeLinha);

console.log(linhas);